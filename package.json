{
  "name": "card-collection-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace @cardx/api run dev",
    "build": "npm run build --workspaces"
  }
}
